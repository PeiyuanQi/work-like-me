interface:
  display_name: "Finish Work"
  short_description: "Check, commit, and push completed code work"
  default_prompt: "Use $swe:finish-work to finish this code work by checking quality, committing, and pushing if requested."
